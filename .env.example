# Servidor (Especificação flunx-channels-api § 2)
PORT=3001
NODE_ENV=production

# Supabase (§ 2.1 - use Service Role Key, não anon key)
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_SERVICE_KEY=eyJ...
# Alternativa: SUPABASE_SERVICE_ROLE_KEY (mesmo valor)

# Evolution API
EVOLUTION_API_URL=https://apiwpp.flunx.com.br
EVOLUTION_API_KEY=sua-api-key-evolution
# Alternativa: EVOLUTION_BASE_URL (mesmo valor que EVOLUTION_API_URL)

# Webhook: URL pública onde a Evolution enviará eventos (§ 2.1)
WEBHOOK_BASE_URL=https://api-canais.flunx.com.br

# Opcional: token para validar webhook (query ?token= ou header X-Webhook-Token)
# WEBHOOK_SECRET_TOKEN=seu-token-secreto

# Opcional: anon key só se quiser usar RLS com JWT do usuário em alguns clientes
# SUPABASE_ANON_KEY=your-anon-key
